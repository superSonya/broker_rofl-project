<Window x:Class="UP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UP"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="40"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Frame x:Name="myframe"/>
        <StackPanel VerticalAlignment="Center" x:Name="Stack">
            <Label Content="Введите ЛОГИН!!!" HorizontalAlignment="Center" FontFamily="Times new Roman" FontSize="14" Margin="0,0,0,1.5"/>
            <TextBox x:Name="login"/>
            <Label Content="Введите ПАРОЛЬ!!!" HorizontalAlignment="Center" FontFamily="Times new Roman" FontSize="14" Margin="0,0,0,1.5"/>
            <TextBox x:Name="password"/>
            <Button Height="40"
                    Width="220"
                    Click="Button_Click"
                    x:Name="btn"
                    Content="login"
                    Background="Green"
                    BorderThickness="4"
                    BorderBrush="Red"/>
        </StackPanel>
    </Grid>
</Window>
